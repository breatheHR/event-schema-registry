{
  "name": "@breathehr/event-schemas",
  "version": "0.3.0",
  "description": "Event schema registry for Breathe",
  "private": false,
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "files": ["events/**/*.json", "VERSION"],
  "scripts": {
    "check-breaking": "node scripts/check-breaking.js"
  },
  "devDependencies": {
    "json-schema-diff": "^1.0.0"
  }
}
